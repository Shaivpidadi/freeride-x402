---
name: Bug report
about: Something doesn't work the way the docs say it should.
labels: bug
---

## What happened

<!-- One line. -->

## Reproducer

<!-- The exact command, request body, or sequence of CLI calls. -->

```bash

```

## Expected vs actual

- **Expected:**
- **Actual:**

## Environment

- `freeride --version`:
- Python version:
- OS / arch:
- Provider keys configured (just names, e.g. `OPENROUTER_API_KEY`):
- Agent / client (Aider / Continue / OpenClaw / curl / openai-python / other):

## Logs

<details>
<summary>Output of <code>freeride serve --verbose</code> at the time of the bug</summary>

```

```
</details>

<!-- If a request returned the wrong thing, include `X-FreeRide-Provider` or the `_freeride_provider` field — that tells us which provider served it. -->
